# Harvested external results

This directory contains standalone mathematical consequences discovered by the programme that are **not dependencies of the programme itself**.

Each result follows a separate trust path:

```text
explicit theorem/certificate
+ standalone proof
+ exact verifier where practical
+ independent literature/priority audit.
```

Current entries:

- [`cor4-extension-complexity/`](cor4-extension-complexity/) — exact certificate that `xc(COR(4))=16`; mathematics replayed exactly in this curation, historical novelty still under specialist audit.

Counterexamples live separately under [`../counterexamples/`](../counterexamples/), including the Boolean/binary source-pair augmentation result.
