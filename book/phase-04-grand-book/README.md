# Phase IV — the Graph-Minors-scale Grand Book

**Current protected/certifiable status: SECOND-RUN AUDITED / CANONICAL PROMOTION PENDING.**  
**Unrestricted bare-semantics status: not claimed.**

Phase IV is the central structural Book. Its goal is one coherent theory satisfying:

1. **G1** canonical exposed compositional object;
2. **G2** nontrivial separator/decomposition theorem;
3. **G3** exact local-to-global reconstruction;
4. **G4** process-sensitive torso structure;
5. **G5** genuine structural order + obstruction/tangle theorem;
6. **G6** broad exact consequences;
7. **G7** an optimal opacity endpoint compatible with Phase I.

The status above is deliberately scoped. The latest synchronized original-ledger snapshot is `ogiekako/test@79f64b1a7634341f66482ae19250aa9fa8677d7d`. At that snapshot, protected G1 has survived three local-main repair passes, and a corrected terminal G7 bundle has survived a provenance-distinct second-run adversarial audit. That audit reports no remaining mathematical blocker in literal G7 for the normalized native protected/certifiable domain `Proc_prot^0`, while explicitly requesting final canonical local-main promotion before writing `REACHED`.

## 1. Why complete state was not the answer

Operational codescent proves exact gluing when complete ambient state is retained. But a complete-state separator is vacuous. The Grand Book requires a smaller natural interface that still preserves legality, recurrence, action, and observer-faithful reconstruction.

The first decisive obstruction is `B_a*B_b`: the factor retractions of

```text
p_a p_b,
p_b p_a
```

coincide, but the next `q_a` has different legality. Cross-factor order is real boundary information.

## 2. GPSH graph-product separator

For

```text
V(Gamma)=A disjoint-union S disjoint-union B,
```

with no edge joining `A` to `B`, GPSH normal form peels a unique alternating sequence of shore frames and one terminal `S`-heap:

```text
H = r_1 ... r_q t.
```

For private frame lists `alpha,beta`, the exact noncopying compatibility object is

```text
P_S(alpha,beta)=C(M_S) x Phase(|alpha|,|beta|),
```

and

```text
C(M_Gamma)
 ~= disjoint-union_(alpha,beta) P_S(alpha,beta).
```

Relative to the fixed private projections, this is the reduced universal quotient of every exact extensional separator code.

Thus separator information is **synchronization information needed to zip private histories**, not merely shared variables.

## 3. State reconstruction is not dynamic typing

A smaller equality-pullback base reconstructs states but can fail to determine the target shared state of an owner-local transaction from the active private fibre. Hence

```text
state reconstruction < transition typing.
```

The exact dynamic interface is dependent/proarrow-like: active-fibre partial action plus passive-fibre transport. This is one concrete reason later realizability is naturally profunctorial rather than simply Cartesian.

## 4. Exact operational codescent

The audited order is

```text
local primitive spans
  -- literal atomic colimit --> global atomic quiver
  -- Free --> exact raw path category
  -- black-box / neutral section / observer quotient only afterward.
```

For a binary owner cover, the relation shadow is the mixed-return star

```text
R_union=(R_1 union R_2)^*.
```

Arbitrarily many recrossings of the glued boundary are part of exact execution, not an implementation artifact.

## 5. No-choice and atlas-valued structure

A bare four-state null process has three nontrivial `2 x 2` charts permuted transitively by `S_4`. Therefore no isomorphism-natural rule can always select one nontrivial chart.

The correct representation-independent response can be an equivariant atlas/moduli of admissible exact charts. But a formal “all charts” groupoid relative to an arbitrary admissibility predicate is not itself G1: useful finite/cofinal, noncopying, process-natural structure still has to be derived.

## 6. Protected G1 repairs

The normalized protected/certifiable domain avoids making decomposition data input. The later G1 audits derive the structural object from fixed native protected operational syntax and repair several tempting shortcuts:

- ordinary `Supp/Hull` is separated from derived GPSH interface structure;
- total literal footprint includes ordinary Hull and storage touch;
- global packed information is charged explicitly and cannot masquerade as a small cut;
- raw decorated-span composition forms the final target before canonicalization;
- gluing ports are not confused with owner-Čech indices;
- all-chart strong monoidality is not claimed;
- the theorem is canonical for the native presentation category, not arbitrary constraint refactorings or bare final semantics.

## 7. G4--G6: torsos, order, consequences

The audited protected/certified package carries full-port recurrent syntax/semantics, storage/action/domain state, observers, grades, provenance, and process-sensitive structural orders. The local taxonomy distinguishes:

```text
TAME / PORT-LOW
  complete-port evaluator certificate;

ACTIVE HARD
  same-process, same-observer hard witness transported by protected order;

ARITHMETIC / OPAQUE
  exact symbolic recurrent residual with explicit interface.
```

Compiler failure alone is never called hardness.

The structural theory supports carrier/treewidth-like and interaction/FIT-like axes on one exact operational structure and recovers substantial exact evaluator/compiler sectors.

## 8. Corrected G7 endpoint

The second-run adversarial audit accepts the corrected G7 mechanism in `Proc_prot^0`:

```text
Face_del(P)=2^Lambda
```

after final raw target formation, with exact free-category face substitution and finite deletion-minimal evaluator cores `EvalCore_D`. Audited `RES-D` laws force load-bearing residual cores to localize to recurrent cyclic/SCC structure. Unresolved cores retain exact symbolic `RA_syn/RA_run` semantics.

Two repairs are essential:

1. An infinite descending chain by itself does not prove absence of a minimal non-LOW semantic face. The corrected doubling-loop theorem instead proves that every nonempty forward-invariant Presburger domain `D` in the calibration has a proper non-LOW forward-invariant Presburger subdomain `2D`.
2. `NO-MAX-DOCTRINE` remains scoped to sound c.e. semantic LOW/HIGH doctrines in the fixed non-c.e. family; it is not a theorem about all complete-port evaluator doctrines.

The endpoint is therefore exact within the zero-new-advice localization grammar generated from exposed native atomic species. Finer class-specific refinements with additional certified data remain legitimate.

## 9. Current stopping line

At the latest synchronized snapshot:

```text
protected/certifiable literal G1--G7:
  no identified mathematical blocker after second-run G7 audit;
  canonical local-main promotion still requested.

arbitrary bare final semantics / arbitrary recodings:
  stronger theorem not claimed.
```

This is the strongest honest current formulation.

## 10. Dossiers and provenance

Read:

- [`../dossiers/phase-IV-operational-codescent.md`](../dossiers/phase-IV-operational-codescent.md)
- [`../dossiers/phase-IV-gpsh-separator.md`](../dossiers/phase-IV-gpsh-separator.md)
- [`../dossiers/phase-IV-no-choice-and-two-stage-semantics.md`](../dossiers/phase-IV-no-choice-and-two-stage-semantics.md)
- [`../dossiers/phase-IV-protected-g1-g7.md`](../dossiers/phase-IV-protected-g1-g7.md)
- [`../../provenance/SOURCE_MAP.md`](../../provenance/SOURCE_MAP.md)
