# Dossier IV-D — protected/certifiable G1--G7 endpoint

**Current status:** `SECOND-RUN AUDITED / CANONICAL PROMOTION PENDING`.

This dossier reflects the latest inspected research-ledger snapshot `79f64b1a7634341f66482ae19250aa9fa8677d7d`.

## Domain

The theorem is scoped to the normalized native protected/certifiable process class `Proc_prot^0`. The input exposes native protected operational syntax, finite atomic species with actual relations/spans and decoders, complete protected transaction/resource structure, observers, and the justified process recodings. It does **not** supply the final layout, owner allocation, port factorization, elimination atlas, hard witness, or evaluator answer.

It is not a theorem about arbitrary bare final semantics or arbitrary constraint refactorings.

## G1 status

Three local-main repair passes derive the protected uncharted object from the native syntax. Among the important repairs:

- ordinary support/Hull is separated from algebraically derived GPSH interfaces;
- total literal footprint includes ordinary Hull and storage touch, avoiding false carrier charts;
- packed global-state information is charged separately so it cannot masquerade as a small cut;
- raw associative decorated-span gluing forms the target process before canonicalization;
- owner-Cech indexing is distinguished from gluing ports;
- all-chart strong monoidality is not claimed.

Literal protected G1 survives these repairs.

## G2--G6 spine

Upstream independently audited components supply exact decomposition/owner allocation, full-port recurrent torso objects, tagged structural obstruction transport, recurrent action/RES-D laws, broad exact recovery sectors, and fibred globalization in the normalized protected/certified scope.

## Corrected G7 endpoint

The provenance-distinct second-run audit accepts, after repairs:

- the final raw-target deletion face lattice `Face_del(P)=2^Lambda`;
- exact free-category face substitution;
- finite canonical deletion-minimal evaluator cores `EvalCore_D`;
- cyclic/strongly-connected localization forced by the audited `RES-D` laws;
- exact symbolic `RA_syn/RA_run` fallback;
- non-heredity of evaluator failure and its separation from G5 hardness;
- a diagnostic symmetric-submodular `kappa_eff,D`;
- recursion-theoretic impossibility of a globally complete c.e.-verifiable finite LOW/HIGH refinement-proof architecture.

Two overclaims are explicitly rejected:

1. an infinite descending non-LOW chain alone does not prove absence of a minimal semantic-domain face; the corrected doubling-loop theorem establishes that every nonempty forward-invariant Presburger domain `D` has a proper non-LOW forward-invariant subdomain `2D` in that natural refinement grammar;
2. `NO-MAX-DOCTRINE` is not extended from the fixed semantic LOW/HIGH family to arbitrary complete-port evaluator doctrines.

## What remains before writing `REACHED`

The second-run handoff says no mathematical blocker remains in literal G7 under the declared domain, but explicitly asks canonical local-main to inspect and promote the corrected bundle. Therefore this curation does not preempt that governance step.

## Provenance

Latest inspected sources:

- `.handoff/20260830-2020-jst-terminal-g7-second-run-audit.md`
- `docs/AUDIT_TERMINAL_G7_SECOND_RUN_20260830.md`
- `docs/TERMINAL_G7_ADVERSARIAL_REPAIR_20260830.md`
- `docs/CANONICAL_EVALUATOR_CORES_G7_20260830.md`
- `docs/G7_NONHEREDITARY_EVALUATOR_BOUNDARY_20260830.md`

in `ogiekako/test@79f64b1...`.
