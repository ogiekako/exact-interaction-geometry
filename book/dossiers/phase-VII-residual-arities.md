# Dossier VII — residual interaction arities

**Status:** finite residual theorem `AUDITED / ACCEPTED`; presentable/nervous shell `KNOWN / PRIOR ART`; full process-generated arity identification remains scope-sensitive.

## Finite residual nerve

For a finite tensor

```text
F : product_(e in E) D_e -> O
```

and `A subseteq E`, define the continuation row

```text
res_A(a) : D_(E-A) -> O,
res_A(a)(b)=F(a,b),
```

and quotient by equality of rows:

```text
Q_A(F) = image(res_A).
```

For disjoint `A,B`, concatenation induces a well-defined gluing map

```text
mu_(A,B) : Q_A x Q_B -> Q_(A union B).
```

The all-subset residual data are extensional: two residual states are equal exactly when all complementary probes give the same outcome. Surjectivity and the generator law reconstruct every `Q_A` as the actual continuation-row quotient of the reconstructed tensor. The root residual recovers `F` itself.

On the stated typed-recoding groupoid, leaf bijections determine every residual-state map, giving full faithfulness.

## Generic world-level arities

For a small wiring category `W`,

```text
A_int = Kar(FinColim(yW)) = PSh(W)_fp
```

is a small dense family of finitely presentable arities. More generally, locally presentable categories are generated by their presentable objects. Finitary/accessibly controlled nervous monads admit fully faithful algebra nerves and essential-image/model descriptions.

These are prior art.

## Project-specific target

The nontrivial statement is not “some dense arities exist”. It is:

> the reduced interfaces forced by the actual process theory form the correct dense/nervous arities for the intended recurrent-process world.

That theorem must identify the world and probes before applying the generic arity machinery.

## Verification

`verification/verify_phase_v_vii_finite_core.py` checks every Boolean three-bit tensor and all disjoint subset products for representative independence and root reconstruction.
