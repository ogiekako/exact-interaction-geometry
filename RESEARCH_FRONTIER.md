# Research frontier

This file contains only **load-bearing next theorems / governance gates**. It is not a wishlist of every possible extension.

Current source-ledger snapshot used here:

```text
ogiekako/test@79f64b1a7634341f66482ae19250aa9fa8677d7d
```

## F0 — canonical promotion of the protected/certifiable G1--G7 theorem

**Status:** second-run adversarial audit accepted the corrected theorem; canonical local-main promotion is still explicitly requested by the source ledger.

The normalized native protected/certifiable domain `Proc_prot^0` now has a corrected G1--G7 bundle whose G7 endpoint survived a provenance-distinct second-run audit with no remaining mathematical blocker identified in the declared domain.

The immediate gate is therefore no longer “invent another G7 mechanism.” It is:

> Reconstruct the corrected integrated bundle in canonical local-main, check that the repaired G1 and G7 hypotheses coexist exactly, and either promote the theorem or name the unique remaining mathematical defect.

A promotion should freeze:

- the exact definition of `Proc_prot^0`;
- the native/protected data supplied as input versus derived chart data;
- the corrected G1 support/Hull/GPSH separation;
- the exact G2--G6 dependency set;
- the repaired G7 deletion-face/evaluator-core theorem;
- the limits of `NO-MAX-DOCTRINE`;
- the distinction between normalized protected/certifiable closure and arbitrary bare-semantic/arbitrary-recoding universality.

When this gate closes, update `STATUS.md`, the Phase-IV dossier, and the theorem index together in one atomic change.

## F1 — strong Phase-VI process-generated realizability

The generic theorem

```text
Cat/B <-> normal-lax (B -> Prof)
```

is prior art. The project-specific theorem is to derive the relevant `Prof` data from the **actual reduced process interfaces**, not from arbitrary full-state witnesses.

Target:

1. define the explicit recurrent-process world and boundary/wiring category;
2. construct the reduced/noncopying interaction stack from Phase-IV interfaces;
3. prove exact reconstruction of raw executions;
4. prove feedback/base-change stability;
5. prove invariance under admitted process recodings;
6. state precisely where complete-state or noncofinal witnesses are excluded.

GPSH already shows why the answer should be dependent/proarrow-like rather than a single shared-state quotient.

## F2 — project-specific Interaction-Arity Theorem

Assuming F1, identify a **small explicit arity category generated by the process-derived interfaces** and prove:

```text
density / full faithfulness,
essential-image characterization,
feedback/composition compatibility,
recoding invariance,
and nervousness/accessibility of the relevant constructor or monad.
```

This must not be discharged by choosing all finitely presentable presheaves after the fact. The theorem is specifically that the programme-generated local probes are the right arities of the intended world.

The NO-CHOICE theorem remains binding: the arity language may be atlas-valued/equivariant rather than a naturally selected single chart.

## F3 — intrinsic interaction decomposition / prime theory

Phase V has a sharp universal positive/negative boundary but not the entire original external-geometry ambition.

High-value target:

> Characterize when an exact interaction/factorization atlas admits a nontrivial prime/decomposition/tangle theory, and identify the exact obstruction created by disjunctive or affine latent synergy when it does not.

A useful theorem should separate at least:

```text
permutable/modular flat sectors,
curved/disjunctive sectors,
shared-latent compression,
prime or irreducible interaction atoms.
```

Do not re-prove generic abstract tangle machinery; prove that the actual interaction separations satisfy (or fail) its hypotheses.

## F4 — standalone consequence harvesting

Continue attacking published open conjectures for which a finite explicit counterexample would be independently publishable.

The preferred pattern is

```text
local / pairwise data look harmless,
but the full factorization atlas is jointly expensive or incompatible.
```

Current target families include:

- Boolean/binary/nonnegative factorization rank;
- extension-complexity product/additivity questions;
- augmentation and basis-exchange principles;
- other local-to-global conjectures vulnerable to collective latent interaction.

Every harvested result follows a separate trust path:

```text
explicit object
+ standalone proof
+ independent verifier
+ fresh literature audit.
```

Programme-wide audit status is discovery provenance, never a correctness dependency.

## Not frontiers by themselves

The following are scaffold, not missing project theorems:

- generalized Grothendieck construction;
- normal-lax `Prof` semantics;
- Conduche/exponentiable pseudo sector;
- Yoneda/density and presentable arities;
- monads-with-arities / nervous monads;
- graph-product equality/Foata normal forms;
- structured decompositions or monoidal width in isolation;
- abstract tangle duality without a process-derived separation system.

The frontier is the **exact bridge from process-derived interfaces into these mature theories**.
